---
/**
 * Wraps rendered markdown body content. Holds the measure (audit §8.4: 60–75
 * characters) so long-form copy never runs the full width of a desktop screen.
 */
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={['prose', className]}>
  <slot />
</div>

<style>
  .prose {
    max-width: var(--measure);
  }

  .prose :global(p),
  .prose :global(ul),
  .prose :global(ol) {
    margin-block: 0 var(--s-3);
  }

  .prose :global(h2) {
    margin-block: var(--s-6) var(--s-2);
  }

  .prose :global(h3) {
    margin-block: var(--s-4) var(--s-1);
  }

  /**
   * Underlined on purpose. Tailwind's preflight removes the browser default,
   * which left prose links distinguishable by colour alone — an axe
   * `link-in-text-block` failure, and WCAG 1.4.1. A link in running text needs
   * a non-colour cue.
   */
  .prose :global(a) {
    color: var(--c-accent);
    text-decoration: underline;
    text-underline-offset: 0.15em;
  }

  .prose :global(blockquote) {
    margin-block: var(--s-4);
    padding-left: var(--s-3);
    border-left: 3px solid var(--c-accent);
    color: var(--c-muted);
  }
</style>
